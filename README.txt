AI Fake Image Detector — B1 Lightweight package
-----------------------------------------------
What you received:
- index.html, style.css, script.js
- assets/logo.svg

How to use:
1. Unzip the project and open index.html in a modern browser (Chrome/Edge/Firefox).
2. Click 'Upload an image' and then 'Analyze Image' to get a score.
3. The tool runs entirely in your browser (no server).

About the detector:
- This is a heuristic B1 detector: fast, small, designed for small businesses.
- It is NOT a replacement for large trained models. For higher accuracy consider integrating a TFJS model or server-side classifier.
